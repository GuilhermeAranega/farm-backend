version: '3.8'
services:
  db:
    image: mysql/mysql-server:latest
    ports:
      - '3306:3306'
    environment:
      - MYSQL_DATABASE=farm-DB
      - MYSQL_USER=root
      - MYSQL_PASSWORD=password
    volumes:
      - mysql-volume:/var/lib/mysql
volumes:
  mysql-volume:
    driver: local
